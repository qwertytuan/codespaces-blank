#include<stdio.h>
int main()
{
    // nhập n mảng
    int n;
   int  a[50];
    printf("Nhập n:");
    scanf("%d", &n);

    // nhập giá trị của mảng a[]
    for (int i = 0; i < n; i++)
    {
        printf("\nnhập a[%d]=", i);
        scanf("%d", &a[i]);
    }
    for(int i=0;i<n/2;i++)
    if(a[i] = a[n-i-1]){
    }
    else{
        printf("\nmang khong doi xung");
        break;
    }
    
}
